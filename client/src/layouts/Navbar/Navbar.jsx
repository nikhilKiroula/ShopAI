import { TopBar, MainNavbar, CategoryBar } from "./";

const Navbar = () => {
  return (
    <header>
      <TopBar />
      <MainNavbar />
      <CategoryBar />
    </header>
  );
};

export default Navbar;